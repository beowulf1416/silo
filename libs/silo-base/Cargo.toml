[package]
name = "silo-base"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "*"

serde = { version = "*", features = ["derive"] }
serde_json = "*"


# projects
silo-plugin = { path = "../silo-plugin" }

# temporary
#plugin-postgres = { path = "../../plugins/plugin-postgres" }
